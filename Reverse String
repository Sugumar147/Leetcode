class Solution {
    public void reverseString(char[] arr) {
        int n=arr.length;
        for(int i=0;i<n/2;i++) {
            char t=arr[i];
            arr[i]=arr[n-i-1];
            arr[n-i-1]=t;
        }
    }
}
